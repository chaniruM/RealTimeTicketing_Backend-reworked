spring.application.name=RealTimeTicketing
server.port=8083

# MongoDB URI to connect to your MongoDB instance
#spring.data.mongodb.uri=mongodb://localhost:27017/realtime_ticketing

# Alternatively, you can specify individual properties if you prefer:
 spring.data.mongodb.host=localhost
 spring.data.mongodb.port=27017
 spring.data.mongodb.database=realtime_ticketing

# Exclude relational database DataSource configuration
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration